// included from both C++ and OpenCL.

// NO LONGER USED -- and if it were would need recoding for 64-bit exponents
//inline u32 bitposToWord(u64 E, u32 N, u32 offset) { return offset * ((u64) N) / E; }
//inline u32 wordToBitpos(u64 E, u32 N, u32 word) { return (word * (E) + (N - 1)) / N; }
